if (CMAKE_VERSION VERSION_LESS @CMAKE_MINIMUM_REQUIRED_VERSION@)
  message (FATAL_ERROR "CMake >= @CMAKE_MINIMUM_REQUIRED_VERSION@ required")
endif (CMAKE_VERSION VERSION_LESS @CMAKE_MINIMUM_REQUIRED_VERSION@)

@PACKAGE_INIT@

include (CMakeFindDependencyMacro)

include ("${CMAKE_CURRENT_LIST_DIR}/aizcv-targets.cmake")
